# 1. 민감한 인증 키 (절대 올리면 안 됨!)
credentials.json
*.pem
*.key

# 2. 너무 무거운 의존성 폴더 (올릴 필요 없음)
node_modules/

# 2-1. 모바일 앱 Vite 빌드 산출물
studycrack-mobile-app/dist/
# 2-1-1. Vite 의존성 최적화 캐시 (빌드 시 생성, 커밋 금지)
studycrack-mobile-app/.vite/

# 3. 환경변수 및 설정파일
.env
serve.json

# 4. 맥/윈도우 시스템 파일 (지저분함)
.DS_Store
Thumbs.db
.idea/

# 5. 에러 로그 등
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 6. 내부 운영 문서와 백엔드 사본 (공개 저장소 추적 금지)
# 이미 추적된 파일은 ignore만으로 보호되지 않으므로 index에서도 제거해야 한다.
/backend-backup/
/CLAUDE.md
/AGENTS.md
/ARCHITECTURE.md
/docs/
# 모바일 앱 모듈 구조 안내 문서(내부용, 공개 레포 비노출)
studycrack-mobile-app/README.md
studycrack-mobile-app/src/README.md

# 7. 기타 지금은 안쓰는 파일 및 폴더들
black/
js/studycrack-mobile.js

# 8. 로컬 개발용 mock 데이터 (절대 커밋 금지)
js/dev-mock.local.js
studycrack-mobile-app/test-results/
studycrack-mobile-app/playwright-report/
studycrack-mobile-app/architecture-baseline.json
